angular.module('app.services', []).factory('Question', function($resource) {
  return $resource('/api/questions/:id', { id: '@id' }, {
    update: {
      method: 'PUT'
    }
  });
}).service('popupService',function($window){
    this.showPopup=function(message){
        return $window.confirm(message);
    }
});
